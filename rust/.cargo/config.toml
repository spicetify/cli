[target.aarch64-pc-windows-msvc]
linker = "rust-lld.exe"

[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"

# [target.x86_64-unknown-linux-gnu]
# # use wild as linker in dev only
# linker = "clang"
# rustflags = ["-C", "link-arg=--ld-path=wild"]

[env]
# Inherited from the upstream cranelift setup. Harmless on stable; kept until
# an aws-lc-sys build proves it unnecessary.
AWS_LC_SYS_NO_U1_BINDINGS = "1"
